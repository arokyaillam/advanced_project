// Re-export the Button component and its types from the Svelte file
export { default as Root } from "./button.svelte";
export { default as Button } from "./button.svelte";

// The types and variants are defined in the Svelte module script
// and can be imported directly from the Svelte file when needed
